Eingereicht:

\begin{verbatim}[1,4,7,8,9]\end{verbatim}

Die Anzahl der Indizes ist richtig?

\begin{quote}\begin{verbatim}Ja.\end{verbatim}\end{quote}

Ihre Lösung ist richtig?

\begin{quote}\begin{verbatim}Nein.\end{verbatim}\end{quote}

\begin{quote}Diese Terme aus Ihrer Lösung sind falsch: \begin{itemize}\item{}$\mathit{yPos(makePos(yPos,posMax,prettyPrint(emptyBoard)))}$\item{}$\mathit{makePos(posMax,yPos(makePos(posMax,prettyPrint(emptyBoard),posMax)),prettyPrint(emptyBoard))}$\end{itemize}\end{quote}  

Alle angegebenen Terme sind korrekt?

\begin{quote}\begin{verbatim}Nein.\end{verbatim}\end{quote}

Die korrekte Lösung ist:\begin{verbatim}[3,4,5,8,9]\end{verbatim}

