Betrachten Sie die folgenden Deklarationen von Konstanten und Operationen:

\begin{itemize}
\item[] $\mathit{emptyBoard} : \mathit{Board}$
\item[] $\mathit{posMax} : \mathit{Int}$
\item[] $\mathit{prettyPrint} : \mathit{Board} \to \mathit{String}$
\item[] $\mathit{makePos} : \mathit{Int} \times \mathit{Int} \times \mathit{String} \to \mathit{Pos}$
\item[] $\mathit{yPos} : \mathit{Pos} \to \mathit{Int}$
\item[] $\mathit{gridValue} : \mathit{Pos} \to \mathit{String}$\end{itemize}Entscheiden Sie für die folgenden Terme, ob sie korrekt im Sinne der oben gegebenen Deklarationen sind:

\begin{itemize}
\item[1.]$\mathit{yPos(makePos(yPos,posMax,prettyPrint(emptyBoard)))}$
\item[2.]$\mathit{makePos(posMax,posMax,posMax,prettyPrint(emptyBoard))}$
\item[3.]$\mathit{makePos(posMax,yPos(makePos(posMax,posMax,prettyPrint(emptyBoard))),prettyPrint(emptyBoard))}$
\item[4.]$\mathit{gridValue(makePos(posMax,posMax,gridValue(makePos(posMax,posMax,prettyPrint(emptyBoard)))))}$
\item[5.]$\mathit{makePos(yPos(makePos(posMax,posMax,prettyPrint(emptyBoard))),posMax,prettyPrint(emptyBoard))}$
\item[6.]$\mathit{gridValue(makePos(emptyBoard,posMax,gridValue(makePos(posMax,posMax,prettyPrint(emptyBoard)))))}$
\item[7.]$\mathit{makePos(posMax,yPos(makePos(posMax,prettyPrint(emptyBoard),posMax)),prettyPrint(emptyBoard))}$
\item[8.]$\mathit{yPos(makePos(posMax,posMax,prettyPrint(emptyBoard)))}$
\item[9.]$\mathit{yPos(makePos(posMax,posMax,gridValue(makePos(posMax,posMax,prettyPrint(emptyBoard)))))}$\end{itemize}Bitte geben Sie Ihre Antwort in Form einer Liste von Zahlen an, die alle korrekten Terme enthält.

Zum Beispiel [1, 2] würde bedeuten, dass nur die Terme 1. und 2. von den angegebenen korrekt sind.

